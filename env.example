# SMTP Configuration
# Copy this file to .env.local and fill in your actual values

# SMTP Server Host (e.g., smtp-mail.outlook.com, smtp.gmail.com)
SMTP_HOST=smtp-mail.outlook.com

# SMTP Port (usually 587 for TLS or 465 for SSL)
SMTP_PORT=587

# Use secure connection (true for SSL/465, false for TLS/587)
SMTP_SECURE=false

# SMTP Username (usually your email address)
SMTP_USER=your-email@example.com

# SMTP Password (App Password for Gmail/Outlook)
# For Outlook/Hotmail: Generate an app password from account security settings
# For Gmail: Use an App Password from Google Account settings
SMTP_PASSWORD=your-app-password-here

# From Email Address (sender email)
SMTP_FROM=your-email@example.com

# To Email Address (recipient email - where you want to receive messages)
SMTP_TO=your-email@example.com

# Gemini API Key (existing)
GEMINI_API_KEY=your-gemini-api-key

